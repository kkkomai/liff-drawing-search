services:
  - type: web
    name: liff-drawing-search
    env: python
    plan: free
    buildCommand: python3 -c "import os; os.makedirs('build', exist_ok=True); open('build/.deployed', 'w').write('deployed')"
    startCommand: python3 search_server.py
