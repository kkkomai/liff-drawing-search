services:
  - type: web
    name: liff-drawing-search
    env: python
    plan: free
    buildCommand: python3 -c "open('.build-marker', 'w').write('built at ' + __import__('datetime').datetime.now().isoformat())" && python3 search_server.py
    startCommand: python3 search_server.py
