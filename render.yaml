services:
  - type: web
    name: liff-drawing-search
    env: python
    plan: free
    runtime: python3.14
    buildCommand: pip install -r requirements.txt 2>/dev/null || true
    startCommand: python3 search_server.py
    healthCheckPath: /health
