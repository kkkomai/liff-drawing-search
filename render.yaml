services:
  - type: web
    name: liff-drawing-search
    env: python
    plan: free
    buildCommand: echo "build" && python3 -c "open('.rb', 'w').write('1')"
    startCommand: python3 search_server.py
