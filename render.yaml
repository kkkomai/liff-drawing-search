services:
  - type: web
    name: liff-drawing-search
    env: python
    plan: free
    buildCommand: echo "rebuild" && python3 -c "import datetime; open('.rb','w').write(datetime.datetime.now().strftime('%Y%m%d%H%M%S'))"
    startCommand: python3 search_server.py
