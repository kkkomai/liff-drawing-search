services:
  - type: web
    name: liff-drawing-search
    env: python
    plan: free
    buildCommand: echo "rebuild trigger" && touch .render-rebuild-trigger
    startCommand: python3 search_server.py
