services:
  - type: web
    name: liff-drawing-search
    env: python
    plan: free
    buildCommand: echo "BUILDING" && python3 search_server.py --version 2>&1 || true
    startCommand: python3 search_server.py
