services:
  - type: web
    name: liff-drawing-search
    env: python
    plan: free
    buildCommand: echo "rebuilding..." && python3 -c "open('.render-rebuild-trigger', 'w').write('rebuild')"
    startCommand: python3 search_server.py
